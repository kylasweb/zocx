export const scheduleTask = (taskId: string, cronExpression: string) => {
  // Implementation would interact with backend API
  console.log(`Scheduling task ${taskId} with cron: ${cronExpression}`);
}; 